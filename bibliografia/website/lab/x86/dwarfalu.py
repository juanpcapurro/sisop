#!/usr/bin/env python3

def get_location(addr, locinfo):
    """Returns (file_name, line_number) for a program address.
    """
    return None


def get_string(addr, elf):
    """Returns the string at ADDR in ELF.
    """
    return "<???>"
